<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Rooms</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/">Home</a></li>
         <li class=<%= controller.controller_name == "rooms" ? "active" : "" %> ><a href="/rooms">Rooms</a></li>
          <li class=<%= controller.controller_name == "bookings" ? "active" : "" %>><a href="/bookings">Bookings </a></li>
          <li class=<%= controller.controller_name == "cities" ? "active" : "" %>><a href="/cities">Cities </a></li>
        
            
            <% if can? :create, Amenity.new,%>
              <li class=<%= controller.controller_name == "amenities" ? "active" : "" %>><a href="/amenities">Amenities</a> </li>
                <%if current_user.role?"admin" %>
            <li class=<%= controller.controller_name == "rooms_authorization_rooms_path" ? "active" : "" %>><a href="/rooms/authorization_rooms">Non authorized rooms </a></li>
            <%end%>
            <%end%>
            


            <% if (can? :update, Booking.new) && (current_user.role?"host") %>
              <li class=<%= controller.controller_name == "rooms_authorization_rooms" ? "active" : "" %>><a href="/rooms/authorization_rooms">Unconfirmed Bookings </a></li>
              <li class=<%= controller.controller_name == "my_rooms" ? "active" : "" %>><a href="/rooms/my_rooms">My Rooms </a></li>
            <%end%>
          </ul>
            <ul class="nav navbar-nav">
                   
                <li class="dropdown">
                <li> <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" >Account <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                  <%if user_signed_in?%>  
                    <li><%= link_to "Account Settings", edit_user_registration_path%></li>
                    <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
                  <%else%>
                    <li><%= link_to "Login", new_user_session_path%></li>
                    <li> <%= link_to "Register", new_user_registration_path %></li> 
                  <%end%> 

                  </ul>
                  </li> 

                </li>
                <%if user_signed_in?%>
                <li class="pull-right"><a href="#"><%= current_user.username%></a></li>
                <%end%> 

            </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>